# Module 2 deliverable — environment report

Status: **Lab 2.1 complete and verified. Labs 2.2–2.4 deferred — this machine must not have
model runtimes installed (limited disk/memory; see `PROGRESS.md`).** Generated by
`scripts/module_02/smoke_test_runtimes.py`, run 2026-07-08.

## Machine profile

- Architecture: arm64 (Apple Silicon) — MLX is available in principle on this hardware
- macOS version: 13.0 (Ventura)
- Python version: 3.12.1 (via `uv`)

## Lab 2.1 — reproducible Python project + dev tool check

**Result: 5 of 6 required tools present.**

| Tool | Required | Found | Path | Version |
|---|---|---|---|---|
| git | yes | yes | `/usr/bin/git` | git version 2.39.2 (Apple Git-143) |
| make | yes | yes | `/usr/bin/make` | GNU Make 3.81 |
| cmake | yes | yes | `/opt/homebrew/bin/cmake` | cmake version 4.3.4 |
| uv | yes | yes | `.../bin/uv` | uv 0.6.8 |
| jq | yes | yes | `/opt/homebrew/bin/jq` | jq-1.7.1 |
| rg (ripgrep) | yes | **no** | n/a | n/a |
| ollama | no (informational) | no | n/a | n/a |
| brew | no (informational) | yes | `/opt/homebrew/bin/brew` | Homebrew 6.0.5 |

**Finding:** `rg` (ripgrep) is not actually installed as a binary on this machine — a
terminal-only shell function shadows the name in interactive sessions, which is why it can
look present at a glance but `shutil.which("rg")` correctly reports it missing. To fix:
`brew install ripgrep`. Recorded here rather than silently working around it, per the
module's "environment can be recreated from README" assessment criterion — the README's
Module 2 install command already lists `ripgrep`.

The reproducible-project part of Lab 2.1 is satisfied by this repo itself: `pyproject.toml`,
`uv.lock`, `Makefile`, `README.md`, and per-module `tests/` directories all exist and are
exercised by `make test` (42 tests passing as of this module).

## Model cache report (theory §10–12)

| Runtime | Cache path | Exists | Size |
|---|---|---|---:|
| Ollama | `~/.ollama/models` | no | n/a |
| Hugging Face (used by mlx-lm) | `~/.cache/huggingface/hub` | **yes** | 2.5 GB |

**Finding:** a Hugging Face cache already exists on this machine with 2.5 GB in it. This
predates this course's work (nothing in this repo has downloaded a model) and is presumably
left over from unrelated prior tooling on this machine. Not touched or cleaned up as part of
this module — flagged here for transparency, not treated as this module's disk usage.

## Labs 2.2–2.4 — runtime smoke tests

All three correctly report a clean, actionable skip rather than fabricating output or
crashing:

| Lab | Runtime | Result |
|---|---|---|
| 2.2 | Ollama | SKIPPED — not reachable at `http://localhost:11434`. Not installed (deliberately — see machine constraint). |
| 2.3 | llama-cpp-python server | SKIPPED — `openai` client package not installed (`llama-cpp-python` itself also not installed). |
| 2.4 | MLX | SKIPPED — `mlx_lm` not importable (not installed). Machine *is* Apple Silicon, so this would work once installed. |

Exact commands to complete each lab are printed by the scripts themselves and are reproduced
in [docs/modules/02_mac_local_ai_development_environment.md](../docs/modules/02_mac_local_ai_development_environment.md).
Full raw output:

```text
## Lab 2.2 — Ollama
Exit code: 1
SKIPPED: Ollama is not reachable at http://localhost:11434.
To complete this lab on a resourced machine:
  brew install ollama
  brew services start ollama          # or: ollama serve
  ollama pull qwen2.5:1.5b
  uv run python scripts/module_02/smoke_test_ollama.py --model qwen2.5:1.5b

## Lab 2.3 — llama-cpp-python server
Exit code: 1
SKIPPED: the `openai` package is not installed. Run: uv add openai

## Lab 2.4 — MLX
Exit code: 1
SKIPPED: `mlx_lm` is not importable.
To complete this lab on a resourced Apple Silicon Mac:
  uv add mlx mlx-lm
  uv run python scripts/module_02/smoke_test_mlx.py
Note: the first run downloads the model from Hugging Face into
~/.cache/huggingface/hub — see model_cache.py for sizing that cache.
```

## What's built and verified this module

| Artifact | Verified how |
|---|---|
| `scripts/module_02/mac_environment_check.py` | 6 unit tests; executed live above (found the real `rg` gap) |
| `scripts/module_02/model_cache.py` | 6 unit tests (including symlink-handling and byte-formatting edge cases); executed live above |
| `scripts/module_02/smoke_test_ollama.py` | 2 unit tests covering the skip path; executed live |
| `scripts/module_02/smoke_test_llamacpp_server.py` | 2 unit tests; executed live |
| `scripts/module_02/smoke_test_mlx.py` | 3 unit tests; executed live |
| `scripts/module_02/smoke_test_runtimes.py` | 2 unit tests on report assembly; executed live to produce this report |
| `scripts/module_02/setup_mac.sh` | Documentation script — reviewed, not executed (would install a runtime) |
| `notebooks/02_mac_environment_setup.ipynb` | **Executed end-to-end** via `jupyter nbconvert --execute` |

23 new unit tests added this module (65 total across the repo including Module 1),
`ruff check .` clean.

## Assessment self-check

- [ ] **One prompt executed through at least two runtimes** — not done; requires an
  installed runtime, deferred to the resourced Mac per the machine constraint.
- [x] **Model files and caches located and documented** — done above; also surfaced a
  pre-existing, unrelated 2.5 GB Hugging Face cache.
- [ ] **Benchmark metrics captured** — deferred with the above.
- [x] **Failure notes recorded for any runtime that does not work on the student's Mac** —
  all three runtimes recorded as not installed, by design, with the exact reason (disk/memory
  constraint) rather than a generic failure.
- [x] **Environment can be recreated from README** — `README.md`'s Module 2 section and
  `scripts/module_02/setup_mac.sh` give the exact commands; the one gap found (`ripgrep`) is
  already covered by the documented `brew install` line.

## Next steps (on the resourced Mac)

```bash
bash scripts/module_02/setup_mac.sh
uv run python scripts/module_02/smoke_test_runtimes.py > /tmp/module_02_raw.md
# then fold the real output into this report, replacing the "SKIPPED" sections above
```
